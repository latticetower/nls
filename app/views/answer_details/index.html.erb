<h1>Listing answer_details</h1>

<table>
  <tr>
  </tr>

<% @answer_details.each do |answer_detail| %>
  <tr>
    <td><%= link_to 'Show', answer_detail %></td>
    <td><%= link_to 'Edit', edit_answer_detail_path(answer_detail) %></td>
    <td><%= link_to 'Destroy', answer_detail, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New answer_detail', new_answer_detail_path %>