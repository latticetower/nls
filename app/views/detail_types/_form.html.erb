<% f.inputs do %>
  <%= f.input :name, :label => "Название вида (краткое)" %>
  <%= f.input :name_long, :label => "Название вида (полное - для печати)" %>
  <%= f.input :group, :label => "группа (для печати)" %>
  <%= f.submit 'Отправить' %>
<% end %>
